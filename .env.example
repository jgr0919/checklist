SECRET_KEY=replace-with-a-long-random-string

DB_USER=checklist
DB_PASS=replace-with-your-db-password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=checklist

# Optional: store uploaded images in S3 instead of local disk
# S3_BUCKET=your-bucket-name
# AWS_DEFAULT_REGION=us-east-2

# Optional: Planning Center Online login integration
# PCO_APP_ID=your-pco-app-id
# PCO_SECRET=your-pco-secret
